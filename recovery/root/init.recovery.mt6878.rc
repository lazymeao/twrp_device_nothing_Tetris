import /init.recovery.mtk_decrypt.rc

on init
    setprop sys.usb.configfs 1
    setprop sys.usb.controller "11201000.usb0"
    setprop sys.usb.ffs.aio_compat 1
    setprop persist.sys.usb.config adb
    setprop sys.usb.config adb
    setprop orangefox.vendor_boot.recovery true
    mkdir /dev/block/bootdevice 0755 root root
    symlink /dev/block/by-name /dev/block/bootdevice/by-name

on boot
    insmod /lib/modules/mtk_disp_notify.ko
    insmod /lib/modules/mediatek-drm-panel-drv.ko
    insmod /lib/modules/sc8541_charger.ko
    insmod /lib/modules/upm6720_charger.ko
    insmod /lib/modules/bootinfo.ko
    insmod /lib/modules/hbt.ko
    insmod /lib/modules/focaltech_tp.ko

on fs && property:ro.debuggable=0
    start adbd

# Keep ADB alive; OrangeFox MTP switching breaks USB gadget on Tetris
on property:sys.usb.config=mtp
    setprop sys.usb.config adb

on property:sys.usb.config=mtp,adb
    setprop sys.usb.config adb

on property:persist.sys.usb.config=mtp
    setprop persist.sys.usb.config adb

on property:persist.sys.usb.config=mtp,adb
    setprop persist.sys.usb.config adb
